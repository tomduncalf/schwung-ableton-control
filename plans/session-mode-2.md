i want to implement session mode when you press row 2 button. ignore the plan/any previous branches as they didnt work out. we previously tried using the v3 framework stuff that move uses and it ended up buggy so instead lets implement our own.

requirements are:

- defualt mode is note mode
- row 2 button toggles into session mode
- in session mode, tracks are horizontal and clips vertical
- we just need to show the first 8 tracks and 4 clips
- slots with a clip present should light up dim. playing clip should light up
